<%= form_for(@purchase) do |f| %>
  <% if @purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% @purchase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :wholesaler %><br>
    <input name="purchase[wholesaler]" id="tags" type="text" onkeyup="getwholeseller()" required>
  </div>

  <div class="field">
    <%= f.label :item_name %><br>
    <input name="purchase[item_name]" id="items" type="text" onkeyup="getitem()" required>
  </div>

  <div class="field">
    <%= f.label :unit_of_measure %><br>
    <input name="purchase[unit_of_measure]" id="uom" type="text" onkeyup="getuom()" required>
  </div>

  <div class="field">
    <%= f.label :quantity %><br>
    <input name="purchase[quantity]" type="integer" step="0.000001" required>
  </div>

  <div class="field">
    <%= f.label :batch_number %><br>
    <input name="purchase[batch_number]" id="batch" type="text" onkeyup="getbatch()" required>
  </div>

  <div class="field">
    <%= f.label :expiry_date %><br>
    <%= f.date_select :expiry_date %>
  </div>

  <div class="field">
    <%= f.label :date_of_purchase %><br>
    <%= f.date_select :date_of_purchase %>
  </div>

  <div class="field">
    <%= f.label :total_price %><br>
    <input name="purchase[total_price]" id="total_price" type="number" step="0.000001" required>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

  <script>

function getwholeseller() {
    $.get("<%= url_for(controller: 'purchases', action: 'wholesaler') %>", function(data, status){
        //   console.log(data);
        $( "#tags" ).autocomplete({
          source: data
        });
    });
};

function getitem() {
    $.get("<%= url_for(controller: 'purchases', action: 'item') %>", function(data, status){
        console.log(data);
        $( "#items" ).autocomplete({
          source: data
        });
    });
};

function getuom() {
    item = document.getElementById("items").value
    $.get("<%= url_for(controller: 'purchases', action: 'uom' ,name:"") %>"+item , function(data, status){
          console.log(data);
        $( "#uom" ).autocomplete({
          source: data
        });
    });
};

function getbatch() {
    item = document.getElementById("items").value
    $.get("<%= url_for(controller: 'stocks', action: 'batch' ,name:"") %>"+item , function(data, status){
          console.log(data);
        $( "#batch" ).autocomplete({
          source: data
        });
    });
};

</script>
